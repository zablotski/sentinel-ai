# Sentinel AI — CI dependencies (lean).
# Everything the headless audit CLI (python -m app.cli) imports at runtime.
# The FastAPI server deps (fastapi/uvicorn) and the torch /
# sentence-transformers stack are intentionally NOT here — the server and the
# embedding classifier are local-only; see requirements.txt for the full set.

pydantic
PyYAML
python-dotenv
httpx
requests

# LangGraph runtime + providers used by the audit graph
langchain-core
langchain-openai
langchain-groq
langgraph
langgraph-checkpoint
langgraph-checkpoint-sqlite
aiosqlite

# Vector DB client (policy RAG + verdict cache)
qdrant-client

# Token counting for tier routing
tiktoken
